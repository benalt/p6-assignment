# Run
With docker installed

```
docker-compose build
docker-compose up
```

App will run at :
[http://localhost:3000](http://localhost:3000)

The Suppliers API runs at :
[http://localhost:8080/api/suppliers](http://localhost:8080/api/suppliers)

The Suppliers API Postgress Database will reset to blank after restart